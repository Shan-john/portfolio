import 'package:flutter/material.dart';

Kheight() => SizedBox(
      height: 15,
    );

Kheight50() => const SizedBox(
      height: 50,
    );


kwidht(size) => SizedBox(
      width: size,
    );
